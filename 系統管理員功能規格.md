# 系統管理員功能 - 規格書

## 系統概述

系統管理員是最高權限的管理者,負責整個平台的運營、監控、審核和管理工作。包括使用者管理、廟宇審核、商品審核、數據監控、系統設定等功能。

---

## 使用者角色定義

### 系統管理員 (System Admin)
- 最高權限管理者
- 可管理所有使用者、廟宇、商品
- 可查看全系統的統計數據
- 可進行系統設定和配置
- 可審核廟方申請和商品上架

---

## 資料庫設計

### 1. admin_users (系統管理員表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | VARCHAR(36) | 管理員 UUID | PRIMARY KEY |
| user_id | VARCHAR(36) | 關聯的使用者 ID | FOREIGN KEY → users.id |
| role | ENUM | 管理員角色 | NOT NULL |
| permissions | JSON | 權限設定 | NULL |
| is_active | BOOLEAN | 是否啟用 | DEFAULT TRUE |
| created_at | DATETIME | 建立時間 | DEFAULT CURRENT_TIMESTAMP |
| created_by | VARCHAR(36) | 建立者 ID | NULL |
| last_login | DATETIME | 最後登入時間 | NULL |

**管理員角色 (role)**:
- `super_admin` - 超級管理員 (所有權限)
- `content_manager` - 內容管理員 (審核商品、廟宇)
- `customer_service` - 客服人員 (處理客訴、退換貨)
- `data_analyst` - 數據分析師 (只能查看數據)

**權限設定 (permissions)** JSON 格式:
```json
{
  "manage_users": true,           // 管理使用者
  "manage_temples": true,          // 管理廟宇
  "manage_products": true,         // 管理商品
  "manage_orders": true,           // 管理訂單
  "view_analytics": true,          // 查看數據分析
  "manage_system_settings": true,  // 管理系統設定
  "manage_admins": false           // 管理其他管理員
}
```

**索引**:
- PRIMARY KEY: `id`
- UNIQUE INDEX: `user_id`
- INDEX: `role`, `is_active`
- FOREIGN KEY: `user_id` REFERENCES `users(id)` ON DELETE CASCADE

---

### 2. temple_applications (廟宇申請表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | VARCHAR(36) | 申請 UUID | PRIMARY KEY |
| applicant_id | VARCHAR(36) | 申請人 ID | FOREIGN KEY → users.id |
| temple_name | VARCHAR(100) | 廟宇名稱 | NOT NULL |
| address | VARCHAR(200) | 廟宇地址 | NOT NULL |
| main_deity | VARCHAR(50) | 主祀神明 | NULL |
| description | TEXT | 廟宇描述 | NULL |
| contact_person | VARCHAR(50) | 聯絡人 | NOT NULL |
| contact_phone | VARCHAR(20) | 聯絡電話 | NOT NULL |
| contact_email | VARCHAR(120) | 聯絡信箱 | NOT NULL |
| proof_documents | JSON | 證明文件 URLs | NULL |
| application_reason | TEXT | 申請理由 | NULL |
| status | ENUM | 申請狀態 | DEFAULT 'pending' |
| reviewed_by | VARCHAR(36) | 審核者 ID | FOREIGN KEY → users.id |
| reviewed_at | DATETIME | 審核時間 | NULL |
| review_notes | TEXT | 審核備註 | NULL |
| created_at | DATETIME | 申請時間 | DEFAULT CURRENT_TIMESTAMP |

**申請狀態 (status)**:
- `pending` - 待審核
- `approved` - 已通過
- `rejected` - 已拒絕
- `on_hold` - 暫緩(需補件)

**索引**:
- PRIMARY KEY: `id`
- INDEX: `applicant_id`, `status`, `created_at`
- FOREIGN KEY:
  - `applicant_id` REFERENCES `users(id)` ON DELETE CASCADE
  - `reviewed_by` REFERENCES `users(id)` ON DELETE SET NULL

---

### 3. product_reviews (商品審核表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | VARCHAR(36) | 審核記錄 UUID | PRIMARY KEY |
| product_id | VARCHAR(36) | 商品 ID | FOREIGN KEY |
| product_type | ENUM | 商品類型 | NOT NULL |
| submitted_by | VARCHAR(36) | 提交者 ID | FOREIGN KEY → users.id |
| status | ENUM | 審核狀態 | DEFAULT 'pending' |
| reviewed_by | VARCHAR(36) | 審核者 ID | FOREIGN KEY → users.id |
| reviewed_at | DATETIME | 審核時間 | NULL |
| review_notes | TEXT | 審核意見 | NULL |
| created_at | DATETIME | 提交時間 | DEFAULT CURRENT_TIMESTAMP |

**商品類型 (product_type)**:
- `general` - 一般商品 (products 表)
- `temple` - 廟宇商品 (temple_products 表)

**審核狀態 (status)**:
- `pending` - 待審核
- `approved` - 已通過
- `rejected` - 已拒絕

**索引**:
- PRIMARY KEY: `id`
- INDEX: `product_id`, `status`, `created_at`
- FOREIGN KEY:
  - `submitted_by` REFERENCES `users(id)` ON DELETE CASCADE
  - `reviewed_by` REFERENCES `users(id)` ON DELETE SET NULL

---

### 4. system_logs (系統日誌表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | BIGINT | 日誌 ID | PRIMARY KEY AUTO_INCREMENT |
| user_id | VARCHAR(36) | 操作者 ID | NULL |
| action | VARCHAR(100) | 操作行為 | NOT NULL |
| target_type | VARCHAR(50) | 目標類型 | NULL |
| target_id | VARCHAR(36) | 目標 ID | NULL |
| ip_address | VARCHAR(45) | IP 位址 | NULL |
| user_agent | VARCHAR(500) | User Agent | NULL |
| request_data | JSON | 請求資料 | NULL |
| response_status | INT | 回應狀態碼 | NULL |
| error_message | TEXT | 錯誤訊息 | NULL |
| created_at | DATETIME | 操作時間 | DEFAULT CURRENT_TIMESTAMP |

**操作行為範例**:
- `user_login`, `user_logout`
- `user_create`, `user_update`, `user_delete`
- `temple_create`, `temple_update`, `temple_delete`
- `product_create`, `product_update`, `product_delete`
- `order_process`, `order_cancel`
- `admin_login`, `admin_action`

**索引**:
- PRIMARY KEY: `id`
- INDEX: `user_id`, `action`, `target_type`, `created_at`
- INDEX: `created_at` (用於定期清理舊日誌)

---

### 5. system_settings (系統設定表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | VARCHAR(36) | 設定 UUID | PRIMARY KEY |
| setting_key | VARCHAR(100) | 設定鍵 | UNIQUE, NOT NULL |
| setting_value | TEXT | 設定值 | NULL |
| setting_type | ENUM | 資料類型 | NOT NULL |
| category | VARCHAR(50) | 設定分類 | NOT NULL |
| description | TEXT | 設定說明 | NULL |
| is_public | BOOLEAN | 是否公開 | DEFAULT FALSE |
| updated_by | VARCHAR(36) | 更新者 ID | FOREIGN KEY → users.id |
| updated_at | DATETIME | 更新時間 | ON UPDATE CURRENT_TIMESTAMP |
| created_at | DATETIME | 建立時間 | DEFAULT CURRENT_TIMESTAMP |

**資料類型 (setting_type)**:
- `string` - 字串
- `number` - 數字
- `boolean` - 布林值
- `json` - JSON 物件

**設定分類 (category)**:
- `general` - 一般設定
- `checkin` - 打卡設定
- `points` - 點數設定
- `products` - 商品設定
- `notifications` - 通知設定
- `security` - 安全設定

**設定範例**:
```json
{
  "default_checkin_points": {
    "value": 10,
    "type": "number",
    "category": "checkin"
  },
  "max_daily_checkins": {
    "value": 10,
    "type": "number",
    "category": "checkin"
  },
  "enable_product_review": {
    "value": true,
    "type": "boolean",
    "category": "products"
  }
}
```

**索引**:
- PRIMARY KEY: `id`
- UNIQUE INDEX: `setting_key`
- INDEX: `category`
- FOREIGN KEY: `updated_by` REFERENCES `users(id)` ON DELETE SET NULL

---

### 6. user_reports (使用者檢舉表)

| 欄位名稱 | 資料型別 | 說明 | 約束 |
|---------|---------|------|------|
| id | VARCHAR(36) | 檢舉 UUID | PRIMARY KEY |
| reporter_id | VARCHAR(36) | 檢舉人 ID | FOREIGN KEY → users.id |
| reported_user_id | VARCHAR(36) | 被檢舉人 ID | FOREIGN KEY → users.id |
| reason | ENUM | 檢舉原因 | NOT NULL |
| description | TEXT | 詳細說明 | NULL |
| evidence_urls | JSON | 證據 URLs | NULL |
| status | ENUM | 處理狀態 | DEFAULT 'pending' |
| handled_by | VARCHAR(36) | 處理者 ID | FOREIGN KEY → users.id |
| handled_at | DATETIME | 處理時間 | NULL |
| action_taken | TEXT | 處理措施 | NULL |
| created_at | DATETIME | 檢舉時間 | DEFAULT CURRENT_TIMESTAMP |

**檢舉原因 (reason)**:
- `spam` - 垃圾訊息
- `fraud` - 詐騙行為
- `harassment` - 騷擾
- `inappropriate_content` - 不當內容
- `other` - 其他

**處理狀態 (status)**:
- `pending` - 待處理
- `investigating` - 調查中
- `resolved` - 已處理
- `dismissed` - 已駁回

**索引**:
- PRIMARY KEY: `id`
- INDEX: `reporter_id`, `reported_user_id`, `status`, `created_at`
- FOREIGN KEY:
  - `reporter_id` REFERENCES `users(id)` ON DELETE CASCADE
  - `reported_user_id` REFERENCES `users(id)` ON DELETE CASCADE
  - `handled_by` REFERENCES `users(id)` ON DELETE SET NULL

---

## API 端點規格

### 🔐 **管理員認證**

#### 1. 管理員登入
```
POST /api/admin/login
```

**說明**: 管理員專用登入端點

**請求參數**:
```json
{
  "email": "admin@example.com",
  "password": "admin_password",
  "admin_code": "optional_2fa_code"
}
```

**回應範例**:
```json
{
  "success": true,
  "data": {
    "admin": {
      "id": "uuid",
      "username": "admin1",
      "role": "super_admin",
      "permissions": {...}
    },
    "access_token": "eyJ0eXAi..."
  }
}
```

---

### 👥 **使用者管理**

#### 2. 取得使用者列表
```
GET /api/admin/users
```

**認證**: 需要 (系統管理員 + manage_users 權限)

**查詢參數**:
- `page`, `per_page`: 分頁
- `search`: 搜尋關鍵字 (username/email)
- `is_active`: 篩選啟用狀態
- `is_admin`: 篩選管理員
- `sort`: 排序 (created_at/blessing_points)
- `start_date`, `end_date`: 註冊日期範圍

**回應範例**:
```json
{
  "success": true,
  "data": {
    "users": [
      {
        "id": "uuid",
        "username": "user123",
        "email": "user@example.com",
        "blessing_points": 500,
        "is_active": true,
        "is_admin": false,
        "total_checkins": 50,
        "total_redemptions": 5,
        "created_at": "2025-01-01T10:00:00",
        "last_active": "2025-01-15T14:30:00"
      }
    ],
    "pagination": {...},
    "summary": {
      "total_users": 1500,
      "active_users": 1200,
      "new_users_this_month": 50
    }
  }
}
```

---

#### 3. 取得使用者詳情
```
GET /api/admin/users/<user_id>
```

**認證**: 需要 (系統管理員)

**回應範例**:
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "uuid",
      "username": "user123",
      "email": "user@example.com",
      "blessing_points": 500,
      "is_active": true,
      "created_at": "2025-01-01"
    },
    "statistics": {
      "total_checkins": 50,
      "unique_temples": 10,
      "total_redemptions": 5,
      "total_points_spent": 600,
      "amulet_count": 2
    },
    "recent_activity": [
      {
        "type": "checkin",
        "temple_name": "龍山寺",
        "timestamp": "2025-01-15T10:30:00"
      }
    ],
    "warnings": [
      {
        "type": "suspicious_activity",
        "description": "異常打卡頻率",
        "timestamp": "2025-01-10"
      }
    ]
  }
}
```

---

#### 4. 更新使用者狀態
```
PUT /api/admin/users/<user_id>/status
```

**認證**: 需要 (系統管理員 + manage_users 權限)

**請求參數**:
```json
{
  "is_active": false,
  "reason": "違反使用條款",
  "notes": "多次檢舉確認為惡意使用者"
}
```

---

#### 5. 調整使用者功德值
```
PUT /api/admin/users/<user_id>/points
```

**認證**: 需要 (系統管理員 + manage_users 權限)

**請求參數**:
```json
{
  "action": "add",  // add/subtract/set
  "amount": 100,
  "reason": "補償系統錯誤"
}
```

---

#### 6. 批量操作使用者
```
POST /api/admin/users/bulk-action
```

**認證**: 需要 (系統管理員 + manage_users 權限)

**請求參數**:
```json
{
  "user_ids": ["uuid1", "uuid2", "uuid3"],
  "action": "deactivate",  // activate/deactivate/delete
  "reason": "批量處理違規帳號"
}
```

---

### 🏛️ **廟宇管理**

#### 7. 取得所有廟宇列表
```
GET /api/admin/temples
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `page`, `per_page`: 分頁
- `search`: 搜尋關鍵字
- `is_active`: 篩選啟用狀態
- `has_admin`: 篩選是否有管理員

**回應範例**:
```json
{
  "success": true,
  "data": {
    "temples": [
      {
        "id": "uuid",
        "name": "龍山寺",
        "address": "台北市萬華區...",
        "is_active": true,
        "admin_count": 2,
        "total_checkins": 5000,
        "total_visitors": 1200,
        "created_at": "2024-01-01"
      }
    ],
    "pagination": {...}
  }
}
```

---

#### 8. 新增廟宇
```
POST /api/admin/temples
```

**認證**: 需要 (系統管理員 + manage_temples 權限)

**請求參數**:
```json
{
  "name": "新廟宇名稱",
  "address": "完整地址",
  "latitude": 25.036889,
  "longitude": 121.499722,
  "main_deity": "主祀神明",
  "description": "廟宇描述",
  "contact_person": "聯絡人",
  "contact_phone": "02-12345678",
  "contact_email": "temple@example.com",
  "nfc_uid": "NFC標籤UID"
}
```

---

#### 9. 更新廟宇資訊
```
PUT /api/admin/temples/<temple_id>
```

**認證**: 需要 (系統管理員 + manage_temples 權限)

---

#### 10. 刪除/停用廟宇
```
DELETE /api/admin/temples/<temple_id>
```

**認證**: 需要 (系統管理員 + manage_temples 權限)

**說明**: 軟刪除,將 is_active 設為 false

---

#### 11. 取得廟宇申請列表
```
GET /api/admin/temple-applications
```

**認證**: 需要 (系統管理員 + manage_temples 權限)

**查詢參數**:
- `status`: 篩選狀態 (pending/approved/rejected)

**回應範例**:
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "applicant": {
        "username": "user123",
        "email": "user@example.com"
      },
      "temple_name": "新廟宇",
      "address": "地址",
      "contact_person": "王主委",
      "contact_phone": "0912345678",
      "status": "pending",
      "created_at": "2025-01-15T10:00:00"
    }
  ]
}
```

---

#### 12. 審核廟宇申請
```
PUT /api/admin/temple-applications/<application_id>/review
```

**認證**: 需要 (系統管理員 + manage_temples 權限)

**請求參數**:
```json
{
  "status": "approved",  // approved/rejected/on_hold
  "review_notes": "審核通過,已建立廟宇資料",
  "create_temple": true,  // 如果通過,是否立即建立廟宇
  "set_as_admin": true    // 是否將申請人設為廟宇管理員
}
```

**商業邏輯**:
- 如果 `status` 為 `approved` 且 `create_temple` 為 true:
  1. 建立新廟宇資料
  2. 如果 `set_as_admin` 為 true,將申請人設為管理員
  3. 發送通知給申請人

---

### 🛍️ **商品管理**

#### 13. 取得所有商品列表
```
GET /api/admin/products
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `type`: 商品類型 (general/temple)
- `category`: 商品分類
- `is_active`: 是否上架
- `stock_status`: 庫存狀態 (in_stock/low_stock/out_of_stock)

**回應範例**:
```json
{
  "success": true,
  "data": {
    "products": [
      {
        "id": "uuid",
        "name": "平安符吊飾",
        "category": "charm",
        "type": "general",
        "merit_points": 100,
        "stock_quantity": 50,
        "is_active": true,
        "redemption_count": 150,
        "created_at": "2025-01-01"
      }
    ],
    "summary": {
      "total_products": 50,
      "active_products": 45,
      "low_stock_count": 5
    }
  }
}
```

---

#### 14. 新增商品
```
POST /api/admin/products
```

**認證**: 需要 (系統管理員 + manage_products 權限)

---

#### 15. 更新商品
```
PUT /api/admin/products/<product_id>
```

**認證**: 需要 (系統管理員 + manage_products 權限)

---

#### 16. 刪除商品
```
DELETE /api/admin/products/<product_id>
```

**認證**: 需要 (系統管理員 + manage_products 權限)

---

#### 17. 批量調整庫存
```
POST /api/admin/products/bulk-stock-update
```

**認證**: 需要 (系統管理員 + manage_products 權限)

**請求參數**:
```json
{
  "updates": [
    {
      "product_id": "uuid1",
      "stock_quantity": 100
    },
    {
      "product_id": "uuid2",
      "stock_quantity": 50
    }
  ]
}
```

---

#### 18. 取得商品審核列表
```
GET /api/admin/product-reviews
```

**認證**: 需要 (系統管理員 + manage_products 權限)

**說明**: 查看待審核的商品(廟方上架的商品)

---

#### 19. 審核商品
```
PUT /api/admin/product-reviews/<review_id>/review
```

**認證**: 需要 (系統管理員 + manage_products 權限)

**請求參數**:
```json
{
  "status": "approved",  // approved/rejected
  "review_notes": "商品符合規範,審核通過"
}
```

---

### 📦 **訂單管理**

#### 20. 取得所有兌換訂單
```
GET /api/admin/redemptions
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `status`: 訂單狀態
- `user_id`: 特定使用者
- `product_id`: 特定商品
- `start_date`, `end_date`: 日期範圍

**回應範例**:
```json
{
  "success": true,
  "data": {
    "redemptions": [
      {
        "id": "uuid",
        "user": {
          "username": "user123",
          "email": "user@example.com"
        },
        "product": {
          "name": "平安符吊飾",
          "type": "general"
        },
        "quantity": 1,
        "merit_points_used": 100,
        "status": "pending",
        "recipient_name": "王小明",
        "phone": "0912345678",
        "address": "台北市...",
        "redeemed_at": "2025-01-15T10:30:00"
      }
    ],
    "summary": {
      "total_orders": 500,
      "pending_orders": 20,
      "total_points_used": 50000
    }
  }
}
```

---

#### 21. 更新訂單狀態
```
PUT /api/admin/redemptions/<redemption_id>/status
```

**認證**: 需要 (系統管理員 + manage_orders 權限)

---

#### 22. 取消訂單(退款)
```
POST /api/admin/redemptions/<redemption_id>/cancel
```

**認證**: 需要 (系統管理員 + manage_orders 權限)

**請求參數**:
```json
{
  "reason": "商品缺貨,無法出貨",
  "refund_points": true
}
```

**商業邏輯**:
- 退還功德值給使用者
- 恢復商品庫存
- 發送通知給使用者

---

### 📊 **數據分析與統計**

#### 23. 取得系統總覽統計
```
GET /api/admin/analytics/overview
```

**認證**: 需要 (系統管理員 + view_analytics 權限)

**查詢參數**:
- `period`: 統計期間 (today/week/month/year)

**回應範例**:
```json
{
  "success": true,
  "data": {
    "users": {
      "total": 1500,
      "active": 1200,
      "new_today": 5,
      "new_this_month": 50
    },
    "temples": {
      "total": 25,
      "active": 24,
      "with_admin": 20
    },
    "checkins": {
      "total": 50000,
      "today": 200,
      "this_month": 5000,
      "avg_daily": 180
    },
    "products": {
      "total": 50,
      "active": 45,
      "low_stock": 5
    },
    "redemptions": {
      "total": 500,
      "pending": 20,
      "this_month": 80,
      "total_points_used": 50000
    },
    "revenue_metrics": {
      "total_points_issued": 500000,
      "total_points_redeemed": 50000,
      "points_in_circulation": 450000
    }
  }
}
```

---

#### 24. 取得使用者成長趨勢
```
GET /api/admin/analytics/user-growth
```

**認證**: 需要 (系統管理員 + view_analytics 權限)

**回應範例**:
```json
{
  "success": true,
  "data": {
    "daily_growth": [
      {
        "date": "2025-01-01",
        "new_users": 10,
        "active_users": 100,
        "retention_rate": 75.5
      }
    ],
    "summary": {
      "growth_rate": 5.2,
      "churn_rate": 2.1
    }
  }
}
```

---

#### 25. 取得打卡熱力圖
```
GET /api/admin/analytics/checkin-heatmap
```

**認證**: 需要 (系統管理員 + view_analytics 權限)

**回應範例**:
```json
{
  "success": true,
  "data": {
    "hourly": [
      {"hour": 0, "count": 20},
      {"hour": 8, "count": 150},
      {"hour": 14, "count": 120}
    ],
    "weekday": [
      {"day": "monday", "count": 180},
      {"day": "saturday", "count": 250}
    ]
  }
}
```

---

#### 26. 取得商品銷售分析
```
GET /api/admin/analytics/product-sales
```

**認證**: 需要 (系統管理員 + view_analytics 權限)

**查詢參數**:
- `period`: 統計期間
- `category`: 商品分類

**回應範例**:
```json
{
  "success": true,
  "data": {
    "top_products": [
      {
        "product_id": "uuid",
        "name": "平安符吊飾",
        "redemption_count": 150,
        "points_earned": 15000,
        "rank": 1
      }
    ],
    "category_performance": [
      {
        "category": "charm",
        "redemption_count": 300,
        "percentage": 40
      }
    ]
  }
}
```

---

#### 27. 匯出數據報表
```
POST /api/admin/analytics/export
```

**認證**: 需要 (系統管理員 + view_analytics 權限)

**請求參數**:
```json
{
  "report_type": "users",  // users/temples/checkins/redemptions
  "format": "excel",       // excel/csv/pdf
  "start_date": "2025-01-01",
  "end_date": "2025-01-31",
  "filters": {...}
}
```

**回應**: 下載檔案

---

### ⚙️ **系統設定**

#### 28. 取得系統設定列表
```
GET /api/admin/settings
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `category`: 設定分類

**回應範例**:
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "setting_key": "default_checkin_points",
      "setting_value": "10",
      "setting_type": "number",
      "category": "checkin",
      "description": "預設打卡獲得的功德值",
      "updated_at": "2025-01-01"
    }
  ]
}
```

---

#### 29. 更新系統設定
```
PUT /api/admin/settings/<setting_id>
```

**認證**: 需要 (系統管理員 + manage_system_settings 權限)

**請求參數**:
```json
{
  "setting_value": "15"
}
```

---

#### 30. 批量更新設定
```
POST /api/admin/settings/bulk-update
```

**認證**: 需要 (系統管理員 + manage_system_settings 權限)

**請求參數**:
```json
{
  "settings": [
    {
      "setting_key": "default_checkin_points",
      "setting_value": "15"
    },
    {
      "setting_key": "max_daily_checkins",
      "setting_value": "10"
    }
  ]
}
```

---

### 🚨 **檢舉處理**

#### 31. 取得檢舉列表
```
GET /api/admin/reports
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `status`: 處理狀態
- `reason`: 檢舉原因

**回應範例**:
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "reporter": {
        "username": "user1"
      },
      "reported_user": {
        "username": "user2"
      },
      "reason": "spam",
      "description": "持續發送垃圾訊息",
      "evidence_urls": ["url1", "url2"],
      "status": "pending",
      "created_at": "2025-01-15T10:00:00"
    }
  ]
}
```

---

#### 32. 處理檢舉
```
PUT /api/admin/reports/<report_id>/handle
```

**認證**: 需要 (系統管理員)

**請求參數**:
```json
{
  "status": "resolved",  // resolved/dismissed
  "action_taken": "已將使用者停權7天",
  "notes": "經查證確實違規"
}
```

---

### 👨‍💼 **管理員管理**

#### 33. 取得管理員列表
```
GET /api/admin/admins
```

**認證**: 需要 (超級管理員)

**回應範例**:
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "user": {
        "username": "admin1",
        "email": "admin@example.com"
      },
      "role": "content_manager",
      "permissions": {...},
      "is_active": true,
      "last_login": "2025-01-15T10:00:00",
      "created_at": "2024-01-01"
    }
  ]
}
```

---

#### 34. 新增管理員
```
POST /api/admin/admins
```

**認證**: 需要 (超級管理員)

**請求參數**:
```json
{
  "user_id": "uuid",
  "role": "content_manager",
  "permissions": {
    "manage_users": false,
    "manage_temples": true,
    "manage_products": true,
    "manage_orders": false,
    "view_analytics": true
  }
}
```

---

#### 35. 更新管理員權限
```
PUT /api/admin/admins/<admin_id>
```

**認證**: 需要 (超級管理員)

---

#### 36. 移除管理員
```
DELETE /api/admin/admins/<admin_id>
```

**認證**: 需要 (超級管理員)

---

### 📋 **系統日誌**

#### 37. 取得系統日誌
```
GET /api/admin/logs
```

**認證**: 需要 (系統管理員)

**查詢參數**:
- `action`: 操作類型
- `user_id`: 特定使用者
- `start_date`, `end_date`: 時間範圍
- `level`: 日誌等級 (info/warning/error)

**回應範例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 12345,
      "user": {
        "username": "admin1"
      },
      "action": "user_update",
      "target_type": "user",
      "target_id": "uuid",
      "ip_address": "192.168.1.1",
      "response_status": 200,
      "created_at": "2025-01-15T10:00:00"
    }
  ]
}
```

---

### 📢 **系統公告**

#### 38. 發布系統公告
```
POST /api/admin/system-announcements
```

**認證**: 需要 (系統管理員)

**請求參數**:
```json
{
  "title": "系統維護公告",
  "content": "系統將於1月20日進行維護...",
  "type": "maintenance",
  "priority": "high",
  "target_users": "all",  // all/temple_admins/specific
  "start_date": "2025-01-20T00:00:00",
  "end_date": "2025-01-20T06:00:00"
}
```

---

## 前端整合範例

```javascript
// 1. 管理員登入
async function adminLogin(email, password) {
  const response = await fetch('http://localhost:5000/api/admin/login', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ email, password })
  });
  
  const data = await response.json();
  if (data.success) {
    localStorage.setItem('admin_token', data.data.access_token);
    return data.data.admin;
  }
  throw new Error(data.error);
}

// 2. 取得系統總覽
async function getSystemOverview() {
  const token = localStorage.getItem('admin_token');
  
  const response = await fetch(
    'http://localhost:5000/api/admin/analytics/overview',
    {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );
  
  return await response.json();
}

// 3. 審核廟宇申請
async function reviewTempleApplication(applicationId, status, notes) {
  const token = localStorage.getItem('admin_token');
  
  const response = await fetch(
    `http://localhost:5000/api/admin/temple-applications/${applicationId}/review`,
    {
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({
        status: status,
        review_notes: notes,
        create_temple: status === 'approved',
        set_as_admin: true
      })
    }
  );
  
  return await response.json();
}

// 4. 調整使用者功德值
async function adjustUserPoints(userId, action, amount, reason) {
  const token = localStorage.getItem('admin_token');
  
  const response = await fetch(
    `http://localhost:5000/api/admin/users/${userId}/points`,
    {
      method: 'PUT',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({ action, amount, reason })
    }
  );
  
  return await response.json();
}

// 5. 批量更新系統設定
async function updateSystemSettings(settings) {
  const token = localStorage.getItem('admin_token');
  
  const response = await fetch(
    'http://localhost:5000/api/admin/settings/bulk-update',
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({ settings })
    }
  );
  
  return await response.json();
}
```

---

## 業務規則

### 權限控制
1. **超級管理員**: 所有權限,可管理其他管理員
2. **內容管理員**: 審核商品、廟宇申請
3. **客服人員**: 處理訂單、檢舉
4. **數據分析師**: 只能查看數據,不能修改

### 審核流程
1. **廟宇申請審核**:
   - 待審核 → 審核通過 → 自動建立廟宇 → 設定申請人為管理員
   - 待審核 → 審核拒絕 → 通知申請人原因
   - 待審核 → 暫緩(需補件) → 通知申請人補件

2. **商品審核**:
   - 廟方上架商品需經過審核才能顯示
   - 審核通過後自動上架
   - 審核拒絕需說明原因

### 日誌記錄
1. 所有管理操作都記錄日誌
2. 包含操作者、操作時間、IP位址
3. 敏感操作(刪除、停權)需要額外確認

### 數據安全
1. 使用者個資需脫敏處理
2. 日誌定期歸檔(保留3個月)
3. 系統設定變更需要審核

---

## 儀表板設計建議

### 首頁儀表板
- **即時數據卡片**: 今日新增使用者、今日打卡數、待處理訂單
- **趨勢圖表**: 使用者成長曲線、打卡趨勢、兌換趨勢
- **待辦事項**: 待審核申請、待處理檢舉、低庫存商品提醒
- **系統狀態**: 資料庫狀態、API 回應時間、錯誤率

### 使用者管理頁面
- 使用者列表(可搜尋、篩選、排序)
- 批量操作功能
- 使用者詳情側邊欄

### 廟宇管理頁面
- 廟宇列表地圖視圖
- 廟宇統計卡片
- 申請審核佇列

### 商品管理頁面
- 商品列表網格視圖
- 庫存警示
- 銷售排行榜

### 數據分析頁面
- 可自訂的圖表
- 數據匯出功能
- 比較分析工具

---

## 安全性考量

### 1. 身份驗證
- 管理員登入使用獨立的認證流程
- 建議啟用雙因素認證 (2FA)
- IP 白名單限制

### 2. 操作審計
- 所有操作記錄日誌
- 敏感操作需要二次確認
- 定期審查管理員操作記錄

### 3. 資料保護
- 使用者個資加密存儲
- API 限流保護
- SQL 注入防護

### 4. 權限控制
- 最小權限原則
- 角色權限矩陣
- 定期審查權限設定

---

## 未來擴充建議

1. **AI 智能分析**
   - 異常行為偵測
   - 自動化審核建議
   - 預測性分析

2. **自動化工具**
   - 定時任務管理
   - 自動化報表
   - 批量處理工具

3. **多語言支援**
   - 後台介面多語言
   - 內容管理多語言

4. **進階分析**
   - 使用者行為分析
   - A/B 測試功能
   - 轉換率分析

5. **整合功能**
   - 第三方支付整合
   - 簡訊/Email 通知服務
   - 雲端備份服務

---

**版本**: 1.0.0  
**最後更新**: 2025-01-15
